from django.urls import path, include
from rest_framework.routers import DefaultRouter

app_name = 'api_gateway'

urlpatterns = [
    # Placeholder for API gateway URLs
    # Will be implemented in the next phase
]